'use strict';

describe('Shopify', function () {

    // initialize a new instance of the filter before each test
    var shopify;

    beforeEach(function () {
        shopify = require('js/shopify');
    });

    it('grabs feed', function() {

    })
    /*
    it('adds a dash after the 5th character of a 9-digit zip code', function () {
        expect(zipcode('981222735')).toBe('98122-2735');
    });

    it('doesn\'t add a dash after on a 5-digit zip code', function () {
        expect(zipcode('98122')).toBe('98122');
    });

    it('does nothing if input is anything other than string', function () {
        expect(zipcode(1234567890)).toBe(1234567890);
    });

    it('does nothing if input doesnt have 9 or 5 digits', function () {
        expect(zipcode('1234567890')).toBe('1234567890');
    });

    it('does nothing if input is undefined', function () {
        expect(zipcode()).toBe();
    });

    it('does nothing if input is null', function () {
        expect(zipcode(null)).toBe(null);
    });*/
});
